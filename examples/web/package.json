{
	"license": "Apache-2.0",
	"author": "Filip Seman",
	"type": "module",
	"scripts": {
		"build": "esbuild main.ts --bundle --minify --format=esm --platform=browser --outdir=./",
		"start": "esbuild --servedir=./"
	},
	"dependencies": {
		"bysquare": "github:xseman/bysquare#develop"
	},
	"devDependencies": {
		"esbuild": "~0.17.0"
	}
}
